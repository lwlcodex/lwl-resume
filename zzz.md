# FC Portal 项目分析、简历表述与面试问答

> 分析日期：2026-09-22  
> 分析范围：当前仓库代码、配置、测试与 Git 历史（含所有本地可见分支）  
> 说明：本文将“项目整体能力”和“个人提交证据”分开描述。简历只应写自己真实参与的部分；带有 `[待补充]` 的业务指标请根据实际数据填写，不要编造。

## 一、项目定位

FC Portal 是面向充电运营业务的企业级 Web 管理平台，采用 Monorepo + 微前端架构承载多个可独立开发、测试和发布的业务应用。平台覆盖门户基础能力、租户与权限、充电运营商（CPO）、服务商（MSP）、充电设备、订单、地图、协议文档等场景。

主应用 `core` 负责登录、门户布局、菜单、页签、路由和子应用装载；`admin`、`cpo`、`msp`、`cdms` 等子应用负责各自业务域；公共包提供请求、权限、国际化、低代码组件、主题与工程能力。

从当前仓库静态统计可看到：

- 10 个应用：`admin`、`cdms`、`core`、`cpo`、`demo-react`、`docs`、`emails`、`map`、`msp`、`templates`。
- 12 个公共包：API、微前端桥接、低代码组件库、国际化、图标、环境、共享业务能力、地图共享能力、工具库、版本插件等。
- 约 216 个一级页面目录、141 条自动生成的路由类型。
- OpenAPI 生成文件包含约 1124 个 HTTP 方法定义。
- 36 个单元测试文件，重点覆盖工具函数、权限、请求、Token 刷新、路由与页签等基础能力。

以上数字表示仓库规模，不等同于个人完成量，也不建议直接写成个人业绩。

## 二、技术栈

| 类别         | 技术与用途                                                                                |
| ------------ | ----------------------------------------------------------------------------------------- |
| 基础框架     | React 19、TypeScript 5.8、React Router 7                                                  |
| 工程化       | pnpm workspace、Turborepo、Vite 6、tsup、ESLint、Prettier、Husky、lint-staged、Commitlint |
| 微前端       | Wujie、wujie-polyfill、主子应用 Bridge                                                    |
| UI 与样式    | Ant Design 5、Tailwind CSS、CSS-in-JS、内部图标库                                         |
| 状态与 Hooks | Valtio、ahooks、React Context、mitt                                                       |
| 表单与表格   | TanStack Form、TanStack Table、Zod、内部配置化组件库                                      |
| 数据请求     | Axios、自研类型安全 API Client、OpenAPI TypeScript 代码生成                               |
| 可视化与地图 | ECharts、Mapbox GL、react-map-gl、高德地图组件                                            |
| 国际化       | 自研 `@pioneer/i18n`、中英文词条及校验/提取脚本                                           |
| 测试         | Vitest、JSDOM、MSW、V8 Coverage                                                           |
| 构建发布     | 多环境 Vite 构建、OSS 静态资源、Jenkins、构建产物版本信息、Bundle Stats                   |
| 自动化脚本   | TSX、Puppeteer Core、路由/API/权限/时区生成及质量统计脚本                                 |

## 三、核心架构与关键链路

### 3.1 Monorepo 分层

```text
fc-portal
├─ apps
│  ├─ core                 门户主应用：登录、布局、菜单、页签、子应用容器
│  ├─ admin                平台与租户管理
│  ├─ cpo                  充电运营商业务
│  ├─ msp                  服务商业务
│  ├─ cdms                 充电设备管理
│  ├─ map                  地图应用
│  ├─ docs / emails        协议文档与邮件页面
│  └─ demo-react/templates 示例和页面模板
├─ packages
│  ├─ api                  类型安全请求客户端
│  ├─ portal-bridge        主子应用通信、鉴权、权限与统一请求
│  ├─ portal-components    配置化表单、表格、详情与操作组件
│  ├─ portal-shared        应用共享适配器和业务组件
│  ├─ i18n / icons         国际化和图标资产
│  ├─ portal-env           环境识别
│  ├─ utils                通用算法与工具
│  └─ vite-plugin-version  构建版本追踪
├─ scripts                 代码生成、校验、统计及业务自动化
├─ shared                  Vite/Vitest/代理等公共工程配置
└─ generated               自动生成的 API、路由和权限类型
```

这种分层让各业务应用可以独立开发与部署，同时复用认证、权限、UI 和工程能力。公共能力以内置 workspace 包共享，减少复制粘贴，并能在一个仓库内完成跨包类型检查。

### 3.2 微前端运行链路

1. 用户进入 `core`，完成登录并加载用户、租户和菜单权限。
2. `core` 根据 `/admin/*`、`/cpo/*`、`/msp/*` 等路由进入统一的 `RouteSub` 容器。
3. 容器从路径解析子应用名称，通过 Wujie `startApp` 加载对应独立应用。
4. 生产环境通过 `/sub-packages/...` 和网关/OSS 路径访问子应用；开发环境由 Vite 代理转发到各子应用开发服务。
5. 主应用通过 `window.parent.portalBridge` 向子应用共享 Token、用户、租户、权限和导航/错误处理能力。
6. Wujie 使用 `alive: true` 保活，门户页签切换时保留子应用状态。

项目对真实微前端兼容问题做了专门处理：

- 注入子应用 `html` 高度，解决 Ant Design 弹层定位范围问题。
- 使用 `EventTargetPlugin` 修复 ShadowRoot 下部分 Ant Design 事件异常。
- 忽略极验脚本，处理子应用加载时的跨域限制。
- 将子应用 `navigator.clipboard` 代理到父窗口，处理 iframe 未聚焦时的剪贴板异常。
- 主应用导航采用 Promise 队列，保证连续导航按序执行；主应用路由完成后等待子应用渲染，再通知子应用内部导航。

### 3.3 登录、Token 刷新与请求链路

`portal-bridge` 是认证和请求的关键边界：

1. 登录后把用户、租户、Access Token、Refresh Token 写入 Bridge，并同步到 Local Storage，支持刷新恢复。
2. 每次业务请求自动补充 `Authorization`、租户、用户、语言等公共请求头。
3. 收到 401 时触发 Refresh Token 请求，成功后仅重试原请求一次。
4. `apiRefreshToken` 由 `lockPromise` 包装，多请求同时 401 时只发起一个刷新请求，其他请求复用同一 Promise，避免刷新风暴。
5. 刷新失败时清理 Token，并通过主应用回调统一进入登录失效流程。
6. 网络错误、业务错误和成功提示由主应用统一处理，子应用仍可以通过参数关闭默认行为。

需要说明的安全边界：Token 当前存放在 Local Storage，优点是微前端共享和刷新恢复简单，但必须重点防范 XSS。更强安全方案是由服务端设置 `HttpOnly + Secure + SameSite` Cookie，同时配合 CSRF 防护；是否能改取决于后端认证和跨域部署方案。

### 3.4 菜单与权限模型

后端返回菜单树后，前端将其转换为四类数据：

- `accesses`：完整权限树。
- `accessMenus`：用于门户菜单展示的菜单和路由节点。
- `accessRoutes`：路由 URL 到布尔值的映射，用于页面级访问控制。
- `accessRecords`：功能编码到布尔值的映射，用于按钮/操作级访问控制。

前端同时通过脚本生成 `Routes` 和 `AccessRecord` 联合类型，使路由、权限码在编译期受到约束。页面操作按钮通过 `getIsRecordAccess` 判断权限；无权时不仅禁用，还提供原因提示。

应正确表述：前端权限控制用于用户体验和减少误操作，不是最终安全边界；后端仍必须校验每个接口的身份与权限。

### 3.5 OpenAPI 类型安全请求

项目通过 `openapi-typescript` 从多个服务的接口文档生成 TypeScript 定义，再由泛型 `ApiClientV2<ApiDefines>` 对请求进行封装：

- API Key 由“路径 + HTTP Method”组成，只能从已生成接口中选择。
- 根据接口定义推导 Path、Query、Body 和 Response 类型。
- GET/DELETE 快捷参数进入 Query，POST/PUT 快捷参数进入 Body。
- 路径参数通过模板替换写入 URL。
- 请求适配器可注入，业务应用统一接入 Bridge 的鉴权、刷新与错误处理。

这套设计解决了手写 URL、参数和返回值类型容易漂移的问题。接口变更后重新生成代码并执行 TypeScript 检查，可以把不少联调问题提前到编译阶段暴露。

### 3.6 配置化组件库

`portal-components` 将后台常见模式抽象为配置驱动组件：

- `PortalQueryTable` 组合查询表单、表格、分页、加载态、行选择清理和操作成功刷新。
- `PortalForm` 基于 TanStack Form + Zod，支持异步初始值、分组/折叠、字段联动、数组字段、统一校验和浮动操作栏。
- `PortalDesc`、`PortalNode` 和标准增删改查操作进一步统一详情与展示逻辑。
- 泛型 Props 把表单值、列表记录和接口数据关联起来，增强页面配置的类型提示。

业务页面主要描述字段、校验、权限和请求，减少重复模板代码，统一交互规范。

### 3.7 自动生成与工程质量

仓库提供多类脚本：

- 扫描页面目录生成子应用路由和全局 `Routes` 类型。
- 从 OpenAPI 文档生成多服务接口类型。
- 从菜单数据生成功能权限码类型，并校验前后端菜单差异。
- 提取、复制、检查国际化词条并统计翻译得分。
- 检查文件大小，统计页面、API、产物体积和脚本使用情况。
- 通过 Vite 插件把 Git Commit、构建模式等信息写入运行时常量和 `version.json`，便于定位线上版本。
- 通过统一 Vite 配置规范静态资源路径、产物 Hash、浏览器兼容、代理和 Bundle Stats。

## 四、可被 Git 证据支持的个人贡献

> 下列结论基于作者名 `liwenlong <e-Wenlong.Li3@geely.com>` 的 20 条可见提交。如果该作者不是你，请不要把本节直接用于简历。

### 4.1 用户反馈工单闭环

对应核心提交：`8b0e6bc1 feat: 用户反馈需求`，单次变更约 635 行新增、17 行删除。

主要工作：

- 完成家充用户反馈列表，支持账号、设备 SN、问题类型、创建时间和工单状态筛选。
- 完成反馈详情与处理页面，支持图片/视频预览、附件展示、解决方案填写和文件上传。
- 基于 ECharts 增加工单状态和问题类型分布统计，并按功能权限决定是否请求、展示统计数据。
- 接入路由权限和按钮级权限，区分查看与处理能力。
- 使用 Zod 校验回复内容、附件数量及上传完成状态，防止未完成文件被提交。
- 处理流程提交后关闭来源页签并刷新列表，形成列表—详情/处理—返回刷新的完整交互闭环。
- 补充国际化词条、API 类型、路由类型和权限编码。

可以概括为：独立交付一个包含查询、统计、详情、处理、上传、权限和国际化的完整后台业务模块，而不只是完成单个页面。

### 4.2 路由与多页签交互优化

对应提交：`139d0809 fix: 路由调整` 等。

主要工作：

- 将反馈详情和处理拆成独立路由，支持可复制、可回退、可纳入权限控制的页面 URL。
- 扩展 `navigateTo` 的参数和页签关闭能力，提交成功后精确关闭处理页，而不是依赖不稳定的“当前页”状态。
- 调整子应用路由生成和开发代理，保证微前端内外导航一致。
- 为核心路由与页签逻辑补充测试。

### 4.3 主题与品牌视觉改版

对应提交：`8169b79b fix: 主题色改版设计`，涉及 36 个文件。

主要工作：

- 更新登录页、欢迎页、充电设备图形和公共状态页资源。
- 调整 Tailwind 设计 Token 与 Ant Design 主题适配，统一主色、图标和组件状态。
- 同步维护 SVG 源文件与生成后的 React 图标组件，避免多应用视觉不一致。
- 优化登录背景图片体积（Git 记录显示约从 148 KB 降至 116 KB）。

### 4.4 家充桩设备名称编辑

对应提交：`c4ee7bd8 fix: 增加家充桩设备名称的编辑功能`。

主要工作：

- 在设备编辑表单增加名称字段并设置必填校验。
- 将名称、标签、经销商、SIM SN 等数据通过类型安全 API 提交。
- 复用配置化编辑组件和按钮级权限控制，保持现有后台交互规范。

### 4.5 多地区协议维护与 PDF 自动化

对应提交：`689349b8 fix: 协议更新`、`35f06781 fix: gen-agreement-pdf脚本`。

主要工作：

- 维护多个国家/地区的英文隐私政策与用户协议页面，并同步路由。
- 编写约 760 行 TypeScript/Puppeteer 自动化工具，扫描协议路由，批量生成 A4 PDF、上传文件并输出数据库 SQL 和结果明细。
- 复用浏览器 Profile 和门户登录态，等待人工完成滑块验证后读取会话；调用用户信息接口验证 Token，失效时自动清理并要求重新登录。
- 使用 `BigInt` 生成主键并校验 MySQL signed BIGINT 上限；对 SQL 字符串进行转义，按 `Asia/Shanghai` 生成时间。
- 对生产环境增加 `--confirm-production` 显式确认，任一步失败立即停止，不生成不完整 SQL。
- 使用 `puppeteer-core` 复用本机 Chrome/Edge，避免安装时额外下载浏览器。

这一经历适合突出“把高重复、易出错的人工流程产品化为可审计自动化工具”。如果有实际耗时对比，可补充：将 `[原人工耗时]` 降至 `[自动化耗时]`，覆盖 `[协议数量]` 份文档。

### 4.6 海外租户与品牌适配

对应提交：`0c6c47d0`、`637c13c2`、`3c6f8d0d` 等。

- 适配海外环境租户创建/更新接口服务变化。
- 完成 JUECLAT 品牌标题、Logo、标签页图标和多个应用 HTML 模板同步更新。
- 调整环境识别和协议 App 名称，兼容海外部署差异。

## 五、推荐简历写法

### 5.1 项目标题

**FC Portal 充电运营管理平台｜前端开发工程师｜[起止时间]**

### 5.2 项目简介

面向充电运营商、服务商和设备管理场景的企业级 Web 平台。基于 React 19、TypeScript、Vite、pnpm workspace 与 Wujie 构建 Monorepo 微前端体系，多个业务子应用可独立开发、测试和发布，并通过公共组件、权限与 API SDK 共享平台能力。

### 5.3 技术栈一行版

React 19、TypeScript、React Router、Wujie、pnpm workspace、Turborepo、Vite、Ant Design、Tailwind CSS、Valtio、TanStack Form/Table、Zod、Axios、OpenAPI、ECharts、Vitest、Puppeteer

### 5.4 推荐职责描述（真实稳妥版）

- 参与 React + TypeScript Monorepo 微前端平台建设，维护 CPO、门户和文档应用，复用统一 API、权限、国际化、主题及配置化组件能力。
- 独立交付用户反馈工单模块，完成多条件查询、状态/类型统计、图像视频预览、附件上传、详情处理、路由页签联动和按钮级权限控制，形成完整业务闭环。
- 基于 ECharts 与 Promise 并行请求实现双维度工单统计；按权限裁剪请求与视图，避免无权限接口调用，并完善加载态和空数据处理。
- 优化微前端路由与多页签交互，将详情/处理拆分为独立路由，支持处理完成后精确关闭来源页签并刷新列表，保证主子应用导航状态一致。
- 推进品牌主题改版，统一 Tailwind Token、Ant Design 主题和跨应用 SVG 图标资源，并将登录背景资源体积降低约 22%。
- 使用 TypeScript + Puppeteer 开发多地区协议自动化工具，串联路由发现、登录态校验、PDF 生成、文件上传和 SQL 输出，并通过生产确认、Token 校验、ID 边界校验及失败中止降低发布风险。
- 适配海外租户接口及多环境品牌配置，维护多国家/地区隐私政策和用户协议页面。

建议从中选 4～5 条，不要全部堆到一段简历中。

### 5.5 精简版（适合一页简历）

- 参与 React 19 + TypeScript + Wujie 的充电运营 Monorepo 微前端平台，维护 CPO、门户和多环境公共能力。
- 独立交付用户反馈工单闭环，覆盖查询统计、媒体预览、附件上传、详情处理、权限和页签路由联动。
- 优化多页签导航与主子应用路由一致性，并完成跨应用主题、Logo 和图标资源改版。
- 使用 Puppeteer 构建协议路由发现、PDF 生成、上传及 SQL 输出自动化流程，加入登录态验证与生产防误操作机制。

### 5.6 有数据后可替换的结果导向版

- 将 `[N]` 个国家/地区、`[M]` 份协议的 PDF 发布流程由人工 `[X 小时]` 缩短至 `[Y 分钟]`，并通过生产确认、Token 校验和失败中止降低误发布风险。
- 交付用户反馈工单模块，上线后支撑 `[日/月工单量]`，将 `[处理链路/人工步骤]` 从 `[A]` 降至 `[B]`。
- 统一 `[N]` 个应用的品牌主题和图标资源，登录背景体积降低约 22%，减少跨应用视觉偏差。

只有能够从监控、项目记录或实际工作中证明的数据才应填写。

## 六、重点项目故事（STAR 回答模板）

### 6.1 用户反馈模块

**S（背景）**：家充用户的问题反馈缺少统一的后台处理入口，运营人员需要查看不同类型的问题、相关媒体和处理状态。

**T（任务）**：在既有 CPO 子应用与微前端规范内，完成从查询、统计、详情到处理提交的全流程，并接入权限、国际化和页签机制。

**A（行动）**：复用 `PortalQueryTable` 和 `PortalForm` 搭建页面；用 ECharts 展示状态/类型分布；对统计接口使用 `Promise.all` 并行请求；根据功能权限裁剪请求；通过 Zod 校验解决方案和附件上传状态；把详情/处理拆为独立路由，保存成功后精确关闭处理页并刷新列表。

**R（结果）**：一次性交付完整工单闭环，代码覆盖 API、权限、路由、国际化和页面交互。可补充实际上线效果 `[待补充]`。

### 6.2 协议自动化工具

**S（背景）**：海外多地区协议需要从 Web 页面逐份生成 PDF、上传文件并生成数据库记录，人工流程重复、耗时且容易出现版本、国家、链接或主键错误。

**T（任务）**：把整个流程自动化，同时处理真实门户的滑块登录、Token 失效和生产环境风险。

**A（行动）**：用正则扫描路由配置并校验页面目录；用 Puppeteer 等待字体和网络稳定后生成 A4 PDF；复用浏览器 Profile，让用户完成滑块后读取会话；调用登录用户接口验证 Token；通过 Fetch/FormData 上传；根据 URL 解析语言、国家和协议类型；使用 BigInt 与 SQL 转义生成安全结果；增加生产确认和失败中止。

**R（结果）**：形成可重复执行、可检查结果、可复用登录态的端到端工具，降低批量协议发布的人工作业量和误操作风险。补充实际时间收益 `[待补充]`。

## 七、面试官可能追问的问题与参考答案

### 7.1 项目与架构

#### Q1：请用两分钟介绍这个项目。

**答：** 这是一个面向充电运营业务的企业级管理平台，覆盖门户、租户权限、运营商、服务商、设备、订单、地图和协议等业务。技术上采用 pnpm workspace + Turborepo 管理 Monorepo，用 React 19、TypeScript 和 Vite 开发；`core` 是主应用，使用 Wujie 装载多个可独立发布的子应用。公共能力沉淀为 workspace 包，包括类型安全 API、主子应用 Bridge、权限、国际化和配置化表单表格。我主要完成了用户反馈工单闭环、路由页签优化、主题改版、海外协议维护，以及协议 PDF 发布自动化工具。

#### Q2：为什么使用微前端，而不是一个 SPA？

**答：** 项目包含多个边界较清晰的业务域，发布节奏和责任团队可能不同。微前端让业务应用可以独立开发、测试和部署，降低单体应用持续膨胀后的发布耦合；主应用统一认证、菜单、布局和导航。代价是运行时复杂度增加，例如样式隔离、路由同步、通信、公共依赖重复和 iframe/Shadow DOM 兼容问题，所以项目用 Bridge、统一 Vite 配置和公共组件包约束这些问题。如果团队很小、业务简单或必须强共享状态，我不会为了架构而使用微前端。

#### Q3：为什么选择 Wujie？它的优缺点是什么？

**答：** Wujie 基于 Web Components + iframe 沙箱，JS 隔离相对彻底，子应用可以独立运行，并支持保活，适合后台多页签场景。项目中 `alive: true` 可以保留页面状态。缺点是 iframe/Shadow DOM 带来弹层定位、事件、剪贴板和第三方脚本兼容问题，调试链路也更长。项目通过 CSS 注入、`EventTargetPlugin`、脚本忽略和父窗口剪贴板代理处理了这些具体问题。

#### Q4：主应用和子应用如何通信？

**答：** 核心是 `portalBridge`。主应用在父窗口创建 Bridge，里面保存用户、租户、Token、权限和主应用能力回调；子应用通过 `window.parent.portalBridge` 读取或更新。数据类能力包括认证和权限，行为类能力包括统一导航、下载动画和错误提示。这样避免每个子应用重复实现登录和全局交互。需要注意 Bridge 必须有明确类型和稳定协议，避免变成任意全局变量集合。

#### Q5：子应用怎样做到既能独立运行，也能嵌入主应用？

**答：** 子应用初始化时都通过相同 Bridge API 取上下文；嵌入时读父窗口创建的数据，独立运行时则根据本地存储和默认配置创建自己的 Bridge。开发时每个应用有独立 Vite 服务，生产时每个应用构建到带 `appName-mode` 的 OSS 前缀。共享包只提供协议和能力，不要求子应用必须依赖主应用源代码。

### 7.2 路由、状态与页签

#### Q6：主子应用路由如何保持一致？

**答：** 主应用先根据完整 URL 更新自己的 React Router 和页签状态；如果目标属于子应用，就等待 Wujie 容器渲染完成，再调用子 iframe 暴露的 `__navigateTo__`。连续导航通过 Promise 队列串行化，避免前一次渲染尚未完成时后一次导航覆盖状态。路径第一段用于确定子应用名称，开发环境再映射到对应代理地址。

#### Q7：为什么详情页和处理页要拆成独立路由？

**答：** 独立路由让页面可刷新、可复制链接、可被浏览器前进后退管理，也能分别做权限控制和页签生命周期管理。如果只用列表内弹窗，复杂详情与上传流程会让列表页面状态过重，也不利于恢复用户上下文。

#### Q8：处理成功后如何保证关闭正确页签并刷新列表？

**答：** 打开处理页时 URL 包含记录标识；提交前保存当前处理页的完整 URL。提交成功后调用统一 `navigateTo` 返回列表，同时传 `refresh: true` 和 `closeTabUrl`。主应用先按 URL 精确移除处理页签，再打开并刷新列表，避免依赖“当前激活页签”产生竞态。

#### Q9：为什么项目使用 Valtio？

**答：** 门户的页签和子应用渲染状态规模不大，但需要在路由、布局和导航工具之间共享。Valtio 的 Proxy 模型写法简洁，适合这种轻量全局状态。表单内部状态则由 TanStack Form 管理，服务端请求状态主要由 ahooks 管理，没有强行把所有状态放到一个全局 Store。

### 7.3 请求、Token 与类型安全

#### Q10：多个请求同时返回 401，怎样避免重复刷新 Token？

**答：** `apiRefreshToken` 外层使用 `lockPromise`。第一次调用会保存正在执行的 Promise，在完成前后续调用都返回同一个 Promise；完成后在 `finally` 中释放。刷新成功后各原请求重试一次，并把 `handle401` 设为 false，防止无限递归；失败则清空 Token 并通知主应用进入登录失效流程。

#### Q11：当前 401 重试方案还有什么可以改进？

**答：** 可以增加请求取消、刷新超时和更明确的错误类型；刷新期间如果用户主动退出，应取消等待请求；对于非幂等请求还要考虑后端是否已经执行成功但响应丢失，不能盲目重试。项目现在只在明确收到 401 且刷新成功后重试一次，已避免无限重试，但还可通过请求 ID 或幂等键增强安全性。

#### Q12：OpenAPI 类型是怎么贯穿请求的？

**答：** 脚本用 `openapi-typescript` 生成服务的 paths/operations 类型。`ApiClientV2` 的泛型参数是接口定义，`request` 的 Key 只能选合法的“路径 + Method”；条件类型再从 Key 推导 Path、Query、Body 和响应类型。这样写错 URL、方法或字段时 TypeScript 会在编译期报错，而不是等联调时才发现。

#### Q13：为什么还需要自研 API Client，不直接使用 Axios？

**答：** Axios 只解决 HTTP 请求，不知道业务接口 Schema。自研 Client 把生成类型、路径参数替换、Query/Body 分配和可注入请求适配器组合起来；Bridge 适配器再统一添加认证头、刷新 Token 和错误处理。底层仍是 Axios，没有重复实现网络库。

#### Q14：Token 存 Local Storage 安全吗？

**答：** 它容易被 XSS 读取，因此不是最安全的方案。项目这样做主要是为了多应用共享和刷新恢复。前端应严格避免不可信 HTML、做好依赖治理和 CSP；更理想的是后端使用 `HttpOnly + Secure + SameSite` Cookie，让 JS 无法直接读取 Token，并根据跨域情况增加 CSRF 防护。最终选择需要认证服务和部署架构共同支持。

### 7.4 权限

#### Q15：项目如何实现页面级和按钮级权限？

**答：** 后端返回带节点类型的菜单树。前端保留完整树，并分别转换成菜单树、路由 Map 和功能码 Map。路由进入前通过 `accessRoutes[pathname]` 判断，按钮通过 `getIsRecordAccess(code)` 判断。Map 查询是 O(1)，页面代码也比较直观。生成脚本进一步把合法路由和权限码生成联合类型，减少硬编码拼写错误。

#### Q16：前端隐藏按钮就能保证安全吗？

**答：** 不能。前端权限只改善体验，用户仍可绕过页面直接请求接口。真正的安全边界必须在网关或后端接口校验 Token、租户、角色和功能权限。前端与后端权限定义应保持一致，并通过菜单/功能码校验脚本及时发现差异。

#### Q17：为什么无权限时有些按钮选择禁用而不是隐藏？

**答：** 禁用并显示原因能让用户知道系统具备该能力，只是当前账号缺少权限，减少“功能不存在”的误解；对敏感能力或会泄露业务信息的入口则应直接隐藏。应根据产品和安全要求选择，不是一律禁用。

### 7.5 表单、表格与业务实现

#### Q18：用户反馈模块最复杂的点是什么？

**答：** 复杂点不在单个 UI，而是跨切面闭环：查询条件要同时驱动列表和统计；统计接口受不同权限控制；详情包含图片、视频和普通附件；处理表单要确认所有文件真正上传成功；最后还要正确刷新列表、关闭处理页签，并同步 API 类型、路由、权限与国际化。

#### Q19：统计请求为什么使用 `Promise.all`？

**答：** 状态统计和类型统计互不依赖，并行请求能把总等待时间从两次请求耗时相加降低到两者较慢者。代码会先判断各自权限，无权限时不发送对应请求。当前 `Promise.all` 的问题是一项失败会让整体失败；如果产品要求另一张图仍可展示，可改为 `Promise.allSettled` 并分别呈现错误状态。

#### Q20：如何保证上传附件全部完成后才能提交？

**答：** Zod 校验同时检查原始文件列表和成功文件列表：至少有一个文件、原始文件状态必须全部为 `done`，而且成功文件数量必须等于原始文件数量。提交时只提取成功结果中的 `fileId`。这比只判断“选了文件”可靠，因为用户可能在上传中或部分失败时点击保存。

#### Q21：为什么要抽象配置化表单和表格？

**答：** 后台页面有大量重复结构，如查询、分页、加载态、操作成功刷新、字段校验和详情展示。抽象后业务页面主要写字段与请求配置，交互更一致，开发成本更低。代价是复杂定制可能受到配置模型限制，因此组件保留 `customRender`、插槽和上下文 API 作为逃生口，避免抽象过度。

#### Q22：TanStack Form + Zod 的价值是什么？

**答：** TanStack Form 管理字段状态、提交和联动，Zod负责可组合、类型友好的数据校验。项目通过 Adapter 统一错误信息，再由 `PortalForm` 处理异步初始值、加载失败和操作栏。这样业务校验可以靠近字段配置，同时保留强类型。

### 7.6 自动化工具

#### Q23：协议自动化脚本的完整流程是什么？

**答：** 先扫描路由配置并验证页面目录，生成可人工调整的 URL 清单；再打开门户等待用户完成账号、密码和滑块；读取 Local Storage 会话并调用用户接口校验 Token；之后无头浏览器逐页等待网络和字体完成，生成 A4 PDF；调用文件接口上传；最后根据 URL 解析语言、国家与协议类型，输出结果 JSON 和 SQL。

#### Q24：为什么登录阶段不是完全无头自动化？

**答：** 门户有滑块验证，强行绕过会增加安全和维护风险。脚本把人工保留在认证环节，用户完成合法登录后才读取已有会话；后续重复操作全部自动化。这是一种 human-in-the-loop 设计，兼顾安全、合规和效率。

#### Q25：脚本怎样防止生产误操作？

**答：** 生产类环境必须显式传 `--confirm-production`；Token 在真正上传前通过线上用户信息接口验证；旧会话失效会清理并重新登录；起始 ID 用 BigInt 并校验数据库上限；SQL 字符串会转义；任何生成或上传失败都会立即停止，并且不输出不完整 SQL，同时保留已生成 PDF 便于排查。

#### Q26：为什么选择 `puppeteer-core`？

**答：** 它不随依赖下载 Chromium，安装更轻，也能复用企业电脑已有的 Chrome 或 Edge。脚本按操作系统查找常见浏览器路径。代价是环境一致性弱一些，进一步工程化可以在 CI 使用固定版本容器和浏览器镜像。

#### Q27：PDF 如何保证内容完整？

**答：** 页面导航使用 `networkidle2`，再显式等待 `document.fonts.ready`，之后切换打印媒体并按 A4、背景打印和固定边距输出。同时检查页面 HTTP 状态。若页面还有懒加载图片，未来可以再增加资源完成检测或页面特定的 ready 标记。

### 7.7 工程化与质量

#### Q28：Monorepo 带来了什么价值和问题？

**答：** 价值是共享类型和组件修改可以原子提交，应用之间依赖用 workspace 协议管理，统一 lint/test/build 配置；Turborepo 可以按任务依赖和缓存提升执行效率。问题是仓库规模和安装成本上升，公共包变更影响面更大，所以需要明确依赖边界、变更检查和按包测试。

#### Q29：项目如何保证多环境静态资源正确加载？

**答：** 每个应用构建时根据 appName 和 mode 生成唯一 OSS Base，如 `/fc-web-dist/{appName}-{mode}/`。`index.html` 不带 Hash 便于覆盖，JS Chunk 带 Hash 用于增量发布和缓存。网关把业务路由指向对应环境的 `index.html`，资源则从该 Base 加载。

#### Q30：如何定位线上运行的是哪个版本？

**答：** 自研 Vite 插件在构建时读取 Git Commit 和构建模式，注入全局 `__VITE_PLUGIN_VERSION__`，非开发构建还输出 `version.json`。排障时可从控制台或静态文件确认版本，避免只根据发布时间猜测。

#### Q31：项目测试主要覆盖什么？还缺什么？

**答：** 当前测试重点在通用工具、树结构处理、Promise 锁、国际化、权限判断、Bridge、API 请求和 Token 刷新、核心路由与页签，适合保障高复用基础逻辑。业务页面端到端测试相对少，后续可为登录、权限菜单、工单处理和跨应用导航增加 Playwright E2E，并在 CI 中执行关键冒烟用例。

#### Q32：如果让你继续优化项目，会做什么？

**答：** 我会优先做四件事：第一，评估用 HttpOnly Cookie 降低 Token 被 XSS 窃取的风险；第二，为微前端 Bridge 做版本化协议和运行时校验；第三，补充核心业务 E2E 和可观测性，包括请求 ID、错误上报和性能指标；第四，检查共享依赖和子应用包体，配合 Bundle Stats、按需加载与缓存策略降低首屏成本。优化顺序要由安全风险、线上数据和真实瓶颈决定。

### 7.8 行为与复盘类问题

#### Q33：你遇到过最棘手的问题是什么？

**答：** 可以讲微前端路由/页签竞态，或协议脚本的认证问题。回答时说明可复现现象、根因、备选方案、最终实现和验证方式。例如：连续导航时主应用已经换路由，但子应用还没完成渲染，导致内部导航丢失；最后通过导航 Promise 队列和 `waitSubRender` 串行保证时序，并补充路由测试。

#### Q34：你做过什么超出普通页面开发的工作？

**答：** 协议 PDF 自动化工具是典型例子。它涉及路由解析、浏览器自动化、真实登录态、Token 校验、文件上传、数据建模、SQL 生成和生产保护，不只是 UI 页面。我把一个跨系统的人工流程拆成明确步骤，并为失败与边界情况设计保护。

#### Q35：这个项目中你有什么不足或可以重做的地方？

**答：** 不应回答“没有”。可以说：协议工具目前主要面向本地人工运行，如果使用频率继续增加，我会补单元测试、结构化日志、断点续跑和固定浏览器环境；工单统计当前 `Promise.all` 是全有或全无，可以按产品要求改成 `allSettled`；业务模块还应补 E2E 测试和上线后的效果指标。

## 八、容易被追问的风险点与回答边界

- 不要说“我搭建了整个微前端架构”，Git 证据更支持“参与并维护微前端平台、优化路由页签”，除非你确实是初始设计者。
- 不要把仓库的 10 个应用、141 条路由、1124 个接口都说成自己完成。
- 不要说“前端权限保证系统安全”，必须强调后端鉴权才是安全边界。
- 不要说“彻底解决并发问题”，说明解决的是同一页面会话内的 Token 刷新并发；多标签页仍可考虑 BroadcastChannel 或服务端策略。
- 不要虚构性能、效率和业务转化指标。没有数据时使用“减少重复操作、降低误操作风险”，并说明后续应通过日志或流程记录量化。
- 若被问及 Local Storage，应承认 XSS 风险并给出 HttpOnly Cookie、CSP 和输入输出安全策略，而不是为现状强行辩护。
- 若被问及自动生成文件，不要把生成代码行数当作个人产出；价值在于生成链路和类型约束。

## 九、面试前建议准备的真实数据

建议从项目记录或实际运行中补充以下信息：

1. 项目参与起止时间、团队规模、你的明确职责边界。
2. 用户反馈模块上线后的日/月工单量、主要使用角色和业务反馈。
3. 协议数量、覆盖国家/地区数、自动化前后耗时、失败率或返工次数变化。
4. 主题改版实际覆盖的环境和应用数量。
5. 你亲自解决过的一个线上故障，包括现象、定位手段和恢复时间。
6. 项目的 CI 检查项、发布频率和回滚方式。
7. 能否展示截图、录屏或脱敏代码；涉及企业代码时先确认保密要求。

## 十、30 秒自我陈述

我主要参与 FC Portal 充电运营管理平台的前端开发。项目是 React + TypeScript 的 Monorepo 微前端体系，主应用通过 Wujie 管理多个独立业务子应用，并沉淀了统一的 API、权限、国际化和配置化组件能力。我独立交付过用户反馈工单闭环，处理过微前端路由和多页签一致性，也完成了跨应用主题改版。除此之外，我用 TypeScript 和 Puppeteer 把多地区协议的页面发现、PDF 生成、上传和 SQL 输出做成自动化工具，并加入登录态校验和生产防误操作机制。我比较擅长把业务页面、平台能力和工程效率结合起来解决问题。

## 十一、代码证据索引

| 能力                   | 代表文件                                                                             |
| ---------------------- | ------------------------------------------------------------------------------------ |
| 微前端装载与兼容处理   | `apps/core/src/pages/route-sub/route-sub.tsx`                                        |
| 主子应用路由与页签     | `apps/core/src/router/router.tsx`、`apps/core/src/stores/store-route-tabs.tsx`       |
| Bridge 数据协议        | `packages/portal-bridge/src/bridge/bridge.ts`、`packages/portal-bridge/src/types.ts` |
| Token 刷新与统一请求   | `packages/portal-bridge/src/bridge-after-api/api-request.ts`、`api-refresh-token.ts` |
| 菜单与功能权限         | `packages/portal-bridge/src/bridge-after-api/api-load-menus.ts`                      |
| 类型安全 API           | `packages/api/src/api-client-v2.ts`、`scripts/gen-apis.ts`                           |
| 配置化表单表格         | `packages/portal-components/src/form/form.tsx`、`query-table/query-table.tsx`        |
| 统一构建与 OSS 路径    | `shared/vite-get-base-config.ts`、`shared/vite-get-oss-base.ts`                      |
| 用户反馈模块           | `apps/cpo/src/pages/e-hc-feedback-list/`                                             |
| 家充桩编辑             | `apps/cpo/src/pages/e-hc-pile-list/op-edit-pile.tsx`                                 |
| 协议自动化（Git 历史） | 提交 `35f06781` 下的 `scripts/gen-agreement-pdf/`                                    |
